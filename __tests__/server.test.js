'use strict';

describe('the truth', () => {
  it('should be true', () => {
    let theTruth = true;
    expect(theTruth).toBe(true);
  });
});

// Need to RTFM about mocking a server and testing

//What I think I need to do is check to make sure all CRUD are up to speed

//Make sure the two monitor events are running